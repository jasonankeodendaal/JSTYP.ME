// @ts-nocheck
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.evird.lm',
  appName: 'MlDrive',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
